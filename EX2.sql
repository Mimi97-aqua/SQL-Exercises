-- Select only the Names of all the Bands
-- Make sure to only return one result from this query, 
-- and that you are not returning any albums 
-- that do not have a release year.

SELECT bands.name AS 'Band Name' 
FROM bands;
